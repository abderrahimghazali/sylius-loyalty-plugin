{% if resource.tier is not null %}
    <span class="badge" {% if resource.tier.color %}style="background-color: {{ resource.tier.color }}; color: white;"{% endif %}>
        {{ resource.tier.name }}
    </span>
{% else %}
    <span class="badge bg-secondary">{{ 'loyalty.ui.no_tier'|trans }}</span>
{% endif %}
